(** Orders submitted to the exchange.

    An order carries everything the matching engine needs to place it in the
    book and enforce price-time priority. Not yet implemented. *)

(* TODO: define the order record (id, side, order_type, price, quantity,
   timestamp) and constructors/validators once the matching engine design is
   settled. *)
