(** Invariant-based tests for the matching engine.

    These will assert matching invariants such as: conservation of quantity
    across fills, price-time priority is respected, partial fills leave the
    correct remainder resting, and market orders never rest on the book. Not yet
    implemented. *)

let () = print_endline "test_matching_engine: no tests yet"
