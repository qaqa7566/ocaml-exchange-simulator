(** Invariant-based tests for the limit order book.

    These will assert structural invariants such as: bids sorted descending and
    asks ascending by price, FIFO ordering preserved within each price level,
    and no crossed book after insertion. Not yet implemented. *)

let () = print_endline "test_order_book: no tests yet"
