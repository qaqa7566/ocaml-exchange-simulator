(** Command-line entry point for the exchange simulator.

    Once the engines are implemented this will wire up replay of a recorded
    event stream through the risk and matching engines and print a metrics
    summary. *)

let () = print_endline "ocaml-exchange-simulator: not yet implemented"
