(** Benchmarks for the exchange simulator.

    Will measure matching-engine throughput (orders/second) and per-order
    processing cost over synthetic and replayed order streams. Not yet
    implemented. *)

let () = print_endline "benchmark: not yet implemented"
